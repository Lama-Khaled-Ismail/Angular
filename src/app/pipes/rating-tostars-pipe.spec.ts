import { RatingTostarsPipe } from '../rating-tostars-pipe';

describe('RatingTostarsPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingTostarsPipe();
    expect(pipe).toBeTruthy();
  });
});
